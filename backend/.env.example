MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=governance_watch

JWT_SECRET=dev-secret-change-me
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

MINISTRY_MATCH_THRESHOLD=85

CORS_ORIGINS=http://localhost:3000

# email:password pairs, comma-separated — add more by appending more pairs.
ADMIN_USERS=admin:admin

ENV=development
